{
  "name": "wordclock-ui",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate": "npm run build && svelteesp32 --engine async --sourcepath dist --outputfile ../src/webapp.h --gzip true --etag true"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "svelte": "^4.2.8",
    "tailwindcss": "^3.4.0",
    "terser": "^5.46.0",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "svelteesp32": "^2.0.0"
  }
}
